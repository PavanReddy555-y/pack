spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/abc
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: experiment
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true